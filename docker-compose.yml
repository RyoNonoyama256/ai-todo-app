services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app
      - todo-data:/app/data
    environment:
      - DATABASE_URL=sqlite:///./data/todos.db
      - CORS_ORIGINS=http://localhost:3000

volumes:
  todo-data:
