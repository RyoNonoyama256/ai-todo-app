# SQLite (デフォルト)
DATABASE_URL=sqlite:///./data/todos.db

# PostgreSQL に切り替える場合:
# DATABASE_URL=postgresql://user:password@localhost:5432/todos

# 許可するフロントエンドのオリジン (カンマ区切り)
CORS_ORIGINS=http://localhost:3000
